"use client";
import {FormEvent,useEffect,useState} from "react";import PageHeader from "@/components/PageHeader";import type{Product}from"@/lib/types";
export default function Products(){const[items,setItems]=useState<Product[]>([]),[q,setQ]=useState(''),[msg,setMsg]=useState('');const load=()=>fetch('/api/products'+(q?'?q='+encodeURIComponent(q):'')).then(r=>r.json()).then(setItems);useEffect(()=>{load()},[]);async function add(e:FormEvent<HTMLFormElement>){e.preventDefault();setMsg('');const f=new FormData(e.currentTarget);const r=await fetch('/api/products',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});const j=await r.json();if(!r.ok)return setMsg(j.error);e.currentTarget.reset();setMsg('Produk berhasil ditambahkan');load()}
return <><PageHeader title="Produk" subtitle="Master SKU dan barcode"/><section className="card"><div className="section-head"><h2>Tambah Produk</h2></div><form className="form-grid" onSubmit={add}><div className="field"><label>SKU *</label><input name="sku" required placeholder="IDM-GRG-001"/></div><div className="field"><label>Nama Produk *</label><input name="name" required placeholder="Indomie Goreng"/></div><div className="field"><label>Barcode</label><input name="barcode" placeholder="8999999001234"/></div><div className="field"><label>Kategori</label><input name="category" placeholder="Makanan"/></div><div className="field"><label>Satuan</label><select name="unit" defaultValue="pcs"><option>pcs</option><option>box</option><option>carton</option><option>pack</option></select></div><div className="field"><label>Minimum stok</label><input name="min_stock" type="number" min="0" defaultValue="0"/></div><div className="span2"><button className="btn btn-blue">Simpan Produk</button></div></form>{msg&&<div className={msg.includes('berhasil')?'notice':'notice error'}>{msg}</div>}</section>
<section className="card" style={{marginTop:18}}><div className="section-head"><h2>Daftar Produk</h2><div><input className="search" value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&load()} placeholder="Cari produk..."/> <button className="btn btn-light" onClick={load}>Cari</button></div></div><div className="table-wrap"><table className="table"><thead><tr><th>Produk</th><th>SKU</th><th>Barcode</th><th>Lokasi</th><th>Stok</th><th>Min.</th></tr></thead><tbody>{items.map(p=><tr key={p.id}><td><strong>{p.name}</strong><div className="muted">{p.category||'-'} · {p.unit}</div></td><td>{p.sku}</td><td>{p.barcode||'-'}</td><td>{p.locations||'-'}</td><td>{p.total_stock}</td><td>{p.min_stock}</td></tr>)}</tbody></table></div></section></>}
