"use client";import {FormEvent,useEffect,useState} from 'react';import {useRouter} from 'next/navigation';import Image from 'next/image';import Link from 'next/link';
export default function Login(){const r=useRouter(),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false),[next,setNext]=useState('/'),[expired,setExpired]=useState(false);useEffect(()=>{const q=new URLSearchParams(window.location.search);setNext(q.get('next')||'/');setExpired(q.get('expired')==='1')},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMsg('');const b=Object.fromEntries(new FormData(e.currentTarget));const x=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(b)});const j=await x.json();setBusy(false);if(!x.ok)return setMsg(j.error||'Gagal login');r.replace(next);r.refresh()}return <main className="auth-page"><section className="auth-card"><div className="auth-brand"><Image src="/wms-acis-logo.png" alt="WMS ACIS" width={210} height={90} priority/><p>Inventory & Warehouse Management</p></div>{expired&&<div className="notice error">Masa trial Anda telah berakhir. Hubungi administrator untuk aktivasi.</div>}{msg&&<div className="notice error">{msg}</div>}<form onSubmit={submit} className="auth-form"><div className="field"><label>Email</label><input name="email" type="email" required autoComplete="email" placeholder="nama@perusahaan.com"/></div><div className="field"><label>Password</label><input name="password" type="password" required autoComplete="current-password"/></div><button className="btn btn-primary auth-submit" disabled={busy}>{busy?'Masuk...':'Masuk ke WMS ACIS'}</button></form><div className="auth-divider"><span>atau</span></div><Link href="/register" className="btn btn-secondary auth-submit">Mulai Trial Gratis 7 Hari</Link><small className="auth-note">Trial tidak memerlukan kartu kredit.</small></section></main>}
