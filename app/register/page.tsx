"use client";import {FormEvent,useState} from 'react';import {useRouter} from 'next/navigation';import Image from 'next/image';import Link from 'next/link';
export default function Register(){const r=useRouter(),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMsg('');const b=Object.fromEntries(new FormData(e.currentTarget));const x=await fetch('/api/auth/register',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(b)});const j=await x.json();setBusy(false);if(!x.ok)return setMsg(j.error||'Gagal membuat trial');r.replace('/');r.refresh()}return <main className="auth-page"><section className="auth-card wide"><div className="auth-brand"><Image src="/wms-acis-logo.png" alt="WMS ACIS" width={210} height={90}/><h1>Trial 7 Hari</h1><p>Coba seluruh alur inventory sebelum berlangganan.</p></div>{msg&&<div className="notice error">{msg}</div>}<form onSubmit={submit} className="form-grid"><div className="field"><label>Nama *</label><input name="name" required/></div><div className="field"><label>Perusahaan *</label><input name="company" required/></div><div className="field span2"><label>Email *</label><input name="email" type="email" required/></div><div className="field span2"><label>Password *</label><input name="password" type="password" minLength={8} required/><small className="field-hint">Minimal 8 karakter.</small></div><div className="span2 modal-actions"><Link href="/login" className="btn btn-secondary">Sudah punya akun</Link><button className="btn btn-primary" disabled={busy}>{busy?'Membuat...':'Mulai Trial 7 Hari'}</button></div></form></section></main>}
