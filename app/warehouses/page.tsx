"use client";import{FormEvent,useEffect,useState}from'react';import PageHeader from'@/components/PageHeader';
export default function Page(){const[w,setW]=useState<any[]>([]),[l,setL]=useState<any[]>([]),[msg,setMsg]=useState('');const load=()=>Promise.all([fetch('/api/warehouses').then(r=>r.json()),fetch('/api/locations').then(r=>r.json())]).then(([a,b])=>{setW(a);setL(b)});useEffect(()=>{load()},[]);async function addWh(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/warehouses',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(f)});const j=await r.json();setMsg(r.ok?'Gudang berhasil dibuat':j.error);if(r.ok){e.currentTarget.reset();load()}}async function addRack(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/locations',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(f)});const j=await r.json();setMsg(r.ok?'Rak berhasil dibuat':j.error);if(r.ok){e.currentTarget.reset();load()}}
return <><PageHeader title="Gudang & Rak" subtitle="Struktur lokasi penyimpanan untuk inventory"/>{msg&&<div className="notice">{msg}</div>}<div className="grid2"><section className="card"><div className="card-head"><h2>⌂ Tambah Gudang</h2></div><form className="form-grid" onSubmit={addWh}><div className="field"><label>Kode</label><input name="code" required placeholder="WH-JKT"/></div><div className="field"><label>Nama Gudang</label><input name="name" required placeholder="Gudang Jakarta"/></div><div className="field span2"><label>Alamat</label><input name="address" placeholder="Opsional"/></div><div className="span2"><button className="btn btn-primary">＋ Tambah Gudang</button></div></form></section><section className="card"><div className="card-head"><h2>⌖ Tambah Rak</h2></div><form className="form-grid" onSubmit={addRack}><div className="field"><label>Gudang</label><select name="warehouse_id" required><option value="">Pilih gudang</option>{w.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select></div><div className="field"><label>Kode Rak</label><input name="code" required placeholder="A-01"/></div><div className="field span2"><label>Nama Rak</label><input name="name" required placeholder="Rak A-01"/></div><div className="span2"><button className="btn btn-secondary">＋ Tambah Rak</button></div></form></section></div><section className="card section-gap"><div className="card-head"><h2>Daftar Lokasi</h2></div><div className="warehouse-cards">{w.map(x=><div className="warehouse-card" key={x.id}><div className="warehouse-icon">⌂</div><div><strong>{x.name}</strong><span>{x.code} · {x.rack_count} rak</span><div className="rack-list">{l.filter(a=>a.warehouse_id===x.id).map(a=><b key={a.id}>{a.code}</b>)}</div></div></div>)}</div></section></>}
