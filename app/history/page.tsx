"use client";import{useEffect,useState}from"react";import PageHeader from"@/components/PageHeader";
export default function History(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch('/api/movements').then(r=>r.json()).then(setRows)},[]);return <><PageHeader title="Riwayat" subtitle="Audit trail seluruh perubahan inventory"/><section className="card"><div className="table-wrap"><table className="table"><thead><tr><th>Waktu</th><th>Produk</th><th>Tipe</th><th>Lokasi</th><th>Qty</th><th>Sebelum → Sesudah</th><th>Catatan</th></tr></thead><tbody>{rows.map(m=><tr key={m.id}><td>{new Date(m.created_at).toLocaleString('id-ID')}</td><td><strong>{m.name}</strong><div className="muted">{m.sku}</div></td><td><span className="badge">{m.type}</span></td><td>{m.location_name||'-'}</td><td>{m.qty}</td><td>{m.before_qty} → {m.after_qty}</td><td>{m.note||'-'}<div className="muted">{m.actor}</div></td></tr>)}</tbody></table></div></section></>}
