"use client";import{FormEvent,useEffect,useState}from'react';import PageHeader from'@/components/PageHeader';
export default function Page(){const[s,setS]=useState<any>(null),[msg,setMsg]=useState('');const load=()=>fetch('/api/aolinx').then(r=>r.json()).then(setS);useEffect(()=>{load()},[]);async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/aolinx',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({...f,enabled:(f as any).enabled==='on'})});setMsg(r.ok?'Konfigurasi AOLINX tersimpan':'Gagal menyimpan konfigurasi');load()}
return <><PageHeader title="AOLINX" subtitle="Connector GudangKu ke Accurate Online"/><div className="integration-hero"><div><span className="eyebrow">INTEGRATION HUB</span><h2>Accurate Online Connector</h2><p>Siapkan koneksi untuk sinkron produk, gudang, dan transaksi inventory ke Accurate Online.</p></div><div className={s?.connected?'status-chip connected':'status-chip'}>{s?.connected?'✓':'!'} {s?.connected?'Credential tersedia':'Belum terhubung'}</div></div><div className="grid2"><section className="card"><div className="card-head"><div><h2>⛓ Konfigurasi API</h2><p>Simpan credential sebagai environment/secret production.</p></div></div>{s&&<form className="form-grid" onSubmit={save}><div className="field span2 toggle-row"><label><input type="checkbox" name="enabled" defaultChecked={s.enabled}/> Aktifkan integrasi AOLINX</label></div><div className="field"><label>Client ID</label><input name="client_id" defaultValue={s.client_id||''} placeholder="Accurate client id"/></div><div className="field"><label>Database ID</label><input name="database_id" defaultValue={s.database_id||''} placeholder="Accurate database id"/></div><div className="field"><label>Client Secret</label><input name="client_secret" type="password" placeholder="Isi untuk memperbarui"/></div><div className="field"><label>Access Token</label><input name="access_token" type="password" placeholder="Isi token jika sudah tersedia"/></div><div className="field span2"><label>Refresh Token</label><input name="refresh_token" type="password" placeholder="Opsional"/></div><div className="span2"><button className="btn btn-primary">✓ Simpan Konfigurasi</button></div></form>}{msg&&<div className="notice">{msg}</div>}</section><section className="card"><div className="card-head"><h2>↻ Rencana Sinkronisasi</h2></div><div className="sync-list"><div>▤<span><strong>Master Produk</strong><small>SKU, nama, barcode, satuan</small></span><b>Ready</b></div><div>▤<span><strong>Gudang & Rak</strong><small>Mapping warehouse Accurate</small></span><b>Ready</b></div><div>↻<span><strong>Barang Masuk / Keluar</strong><small>Sinkron transaksi inventory</small></span><b>Next</b></div><div>↻<span><strong>Transfer Gudang</strong><small>Mapping pemindahan stok</small></span><b>Next</b></div></div><div className="integration-note"><strong>Catatan keamanan</strong><p>Jangan commit token Accurate ke GitHub. Untuk production, credential sebaiknya disimpan di Railway Variables dan connector menggunakan OAuth resmi Accurate Online.</p></div></section></div></>}
